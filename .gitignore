node_modules/
.expo/
dist/
web-build/

# iOS native project is checked in (matches Android's pattern -- both
# carry hand-written native modules). Build outputs are excluded by
# ios/.gitignore.

# Build artifacts
assets/bare-universal.bundle
assets/bare-ios.bundle
assets/bare-ios-sim.bundle
assets/app-ui.bundle
*.apk
*.aab
*.ipa
*.dmg
*.exe

# Local-only docs (per CLAUDE.md)
TODO.md
DONE.md

# Release pipeline (local-only, mirrors PearCal/PearGuard pattern).
# scripts/app.conf is tracked since it's app-config rather than credentials.
scripts/release.sh
scripts/ios-appstore.sh
scripts/frame-android-screenshots.sh
scripts/assets/
scripts/.env
.env

# Store-listing assets and the Zapstore publish manifest live locally.
# Screenshots, framed bezel composites, store-listing copy, icon and
# feature-graphic exports are all bulky binary or PR-style content that
# the release scripts regenerate; no need to bloat the public repo.
metadata/
store/
zapstore.yaml

# Logs / OS junk
*.log
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/
