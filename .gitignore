node_modules/
.expo/
dist/
web-build/

# iOS native project — still regenerated by `npx expo prebuild` until
# we add iOS work. Android is checked in (has hand-written native
# modules); see android/.gitignore for build-output exclusions.
ios/

# Build artifacts
assets/bare-universal.bundle
assets/bare-ios.bundle
assets/bare-ios-sim.bundle
assets/app-ui.bundle
*.apk
*.aab
*.ipa
*.dmg
*.exe

# Local-only docs (per CLAUDE.md)
TODO.md
DONE.md

# Logs / OS junk
*.log
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/
